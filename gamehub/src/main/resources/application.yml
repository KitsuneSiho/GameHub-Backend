spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gamehub?useSSL=false&serverTimezone=Asia/Seoul&Encoding=UTF-8
    username: root
    password: qwe123
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    cache: false

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
